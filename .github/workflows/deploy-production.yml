name: Deploy Production

on:
  push:
    branches:
      - prod   # change to your production branch (e.g. master, production)
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production   # add required reviewers in GitHub for manual approval

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test -- --passWithNoTests

      - name: Build NestJS
        run: npm run build

      - name: Create deployment artifact
        run: |
          tar -czf deploy.tar.gz \
            dist \
            package.json \
            package-lock.json \
            ecosystem.config.js

      - name: Copy artifact to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "deploy.tar.gz"
          target: "/tmp/"

      - name: Deploy on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          envs: GITHUB_SHA
          script: |
            set -e
            APP_DIR=/var/www/app-production
            RELEASE_DIR=$APP_DIR/releases/${GITHUB_SHA}
            CURRENT=$APP_DIR/current

            mkdir -p "$RELEASE_DIR"
            tar -xzf /tmp/deploy.tar.gz -C "$RELEASE_DIR"
            rm /tmp/deploy.tar.gz

            ln -sf $APP_DIR/shared/.env "$RELEASE_DIR/.env"

            cd "$RELEASE_DIR"
            npm ci --omit=dev

            ln -sfn "$RELEASE_DIR" "$CURRENT"

            cd "$CURRENT"
            pm2 reload ecosystem.config.js --only app-production --update-env || \
              pm2 start ecosystem.config.js --only app-production
            pm2 save

            cd $APP_DIR/releases
            ls -1tr | head -n -5 | xargs -r rm -rf
        env:
          GITHUB_SHA: ${{ github.sha }}
